/* Vendored stub — the upstream file is generated by CMake (GenerateExportHeader)
   for shared-library builds. We build miniz sources statically inside jkctl,
   so the export macro is empty. (P4 SDK C 후보 패키지 매니저, docs/51.) */
#ifndef MINIZ_EXPORT_H
#define MINIZ_EXPORT_H

#define MINIZ_EXPORT

#endif /* MINIZ_EXPORT_H */