#include <JKAudioCommand.h>

namespace jk {

// AudioCommand is a plain data structure used as a binary payload across the
// audio message bus. This translation unit exists to satisfy the build system
// when the header is added to the source list.

} // namespace jk
