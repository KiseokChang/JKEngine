# Desktop Agent M2a — 커맨드 바 MVP 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Alt+Space로 열리는 ImGui 커맨드 팔레트 — 슬래시 결정적 커맨드가 Desktop Agent API(M1의 도구 집합)를 자기 창 연결 위에서 호출하고, 이벤트 피드(알림 센터 최소판)가 실시간 데스크탑 이벤트를 보여준다.

**Architecture:** 팔레트는 새 앱 `jkapp_palette.dll`(ImGui, taskmgr 템플릿)로서 **일반 창 클라이언트**다 — 자기 표면을 갖고, 같은 연결 위에서 AgentQuery/AgentReply/AgentEvent(와이어 17~20)를 주고받는다("하나의 API, 여러 얼굴": MCP/jkagentd와 동일 도구). 서버는 (1) 창 클라이언트의 이벤트 구독을 허용하고 (2) close_window에 서버 측 permissions.json 관문을 둔 뒤 (3) Alt+Space로 팔레트를 스폰/재활성화한다.

**Tech Stack:** JKClientApplication + ImGui (imgui_impl_jkwindow), JKClientSurface 파이프 IPC, quickjs 기반 JKAgentJson(jkcore), PowerShell probes.

**Spec:** `docs/superpowers/specs/2026-09-09-desktop-agent-platform-design.md` §6.2 (커맨드 바), §5 (권한), §9 (undo = 레이아웃 스냅샷), §7 (알림 센터 최소판). 사용 문서: `docs/29_desktop_agent_m1.md`.
**이번 스코프 밖(사용자 결정):** 트리거 스크립트(M2b), 내부 에이전트 채팅창(JKWindow/SDL 밖 — 별도 계획), 자연어 위임.

## Global Constraints

- **두 트랙 원칙** (docs/23 §11): 팔레트는 ImGui 유틸리티 트랙 — `imgui` CMake 타깃 사용, 절대 jkcore에 추가 금지. taskmgr(`ClientTaskmgrApp`)을 템플릿으로 삼는다.
- **ImGui 폰트는 ASCII만** — UI 문자열은 전부 영어. 한글은 렌더링되지 않는다 (docs/26 백로그).
- `JKWindowServer.cpp`는 windows.h를 include하지 않는다 — Win32는 `extern "C" __declspec(dllimport)` 수동 선언 (GetModuleFileNameA/CreateDirectoryA 선언 존재).
- `HandleAgentQuery`/`PushAgentEvent`는 `clientsMutex_` 보유 전제 (ProcessClientMessage에서 호출). 보유 상태에서 FindClientById 같은 내부 락킹 헬퍼 호출 금지 — clients_ 직접 순회.
- `FocusClient`는 락 없이 동작 (호출자가 clientsMutex_ 보유 관례).
- 빌드: `cd engine && export PATH="/c/msys64/ucrt64/bin:$PATH" && cmake --build build --target <target>`; 실행/스모크는 engine/build에서.
- 프로브 관례: `engine/tools/probes/*.ps1`, Start-Process → sleep → 판정 → Stop-Process, exit code = pass/fail.
- 커밋: task-per-commit, `Co-Authored-By: Claude Code <noreply@anthropic.com>`, main 직접 push.
- 서버 측 상태 파일은 모두 `<exeDir>\state\` (StateDir() 관례); permissions.json은 exe 옆 (jkagentd와 같은 빌드 디렉터리 = 같은 파일).
- 팔레트 파괴적 커맨드(close/restore) 전에 자동 `save_layout "pre_undo"` — undo 칩의 기반 (스펙 §9 "undo(레이아웃 스냅샷)").

---

### Task 1: 서버 — 창 클라이언트 이벤트 구독 허용 + close_window 서버 관문

**Files:**
- Modify: `engine/src/server/JKWindowServer.cpp` (PushAgentEvent ~1417행, HandleAgentQuery close_window ~1287행, StateDir 근처에 헬퍼)
- Modify: `engine/include/server/JKWindowServer.h` (헬퍼 선언)

**Interfaces:**
- Consumes: M1의 `jk::agent::AgentJson` (GetStr), M1의 close_window 분기.
- Produces: `bool JKWindowServer::AgentToolAllowed(const std::string& tool) const` — Task 4의 팔레트 close가 거쳐 가는 서버 관문. `PushAgentEvent`가 창 클라이언트(구독 선언한)에게도 이벤트를 보냄 — Task 5의 이벤트 피드 전제.

- [ ] **Step 1: PushAgentEvent 필터 완화**

현재(1417행):
```cpp
if (c && c->IsControlOnly() && c->AgentEventSubscriber() && !c->IsDisconnected()) {
```
control-only 전용이라 창 클라이언트가 구독해도 못 받는다. 창 클라이언트도 `AgentEventSubscribe`를 보내면 구독자가 되도록 완화:
```cpp
// Any connected client that opted in gets events — window clients (the
// palette, future trigger hosts) included. Control-only agent connections
// keep declaring the flag at connect time; window clients send the same
// message later through ProcessClientMessage.
if (c && c->AgentEventSubscriber() && !c->IsDisconnected()) {
```

- [ ] **Step 2: AgentToolAllowed 구현 (서버 측 권한 관문)**

`StateDir()` 근처에 추가. permissions.json은 exe 옆 — jkagentd가 읽는 파일과 **같은 파일**(두 exe가 같은 빌드 디렉터리)이므로 한 곳에 한 규칙:
```cpp
// M2a server-side gate (spec §5): the broker (jkagentd) gates its own tool
// calls, but any connected face can also send AgentQuery directly (the
// palette does). close_window is denied by default; <exeDir>\permissions.json
// — the same file the broker reads — is the approval act.
bool JKWindowServer::AgentToolAllowed(const std::string& tool) const {
    char exePath[1024] = {};
    GetModuleFileNameA(nullptr, exePath, sizeof(exePath));
    std::string dir = exePath;
    const size_t slash = dir.find_last_of("\\/");
    if (slash != std::string::npos) dir = dir.substr(0, slash);
    const std::string path = dir + "\\permissions.json";
    std::FILE* f = std::fopen(path.c_str(), "rb");
    const bool defaultAllowed = (tool != "close_window");
    if (!f) return defaultAllowed;
    char buf[4096] = {};
    const size_t n = std::fread(buf, 1, sizeof(buf) - 1, f);
    std::fclose(f);
    buf[n] = '\0';
    jk::agent::AgentJson perm(buf);
    std::string value;
    if (!perm.ok() || !perm.GetStr(tool.c_str(), value)) return defaultAllowed;
    return value == "allow";
}
```
헤더에 `bool AgentToolAllowed(const std::string& tool) const;` 선언.

- [ ] **Step 3: close_window 분기에 관문 적용**

```cpp
if (target && !target->IsControlOnly() && AgentToolAllowed("close_window")) {
    ipc::WriteMessage(target->Transport(), ipc::MsgType::Close,
                      std::vector<uint8_t>{});
    reply = "{\"ok\":true}";
} else if (target) {
    reply = "{\"ok\":false,\"error\":\"permission_denied\"}";
} else {
    reply = "{\"ok\":false,\"error\":\"window_not_found\"}";
}
```

- [ ] **Step 4: 빌드 + 회귀**

Run: `cmake --build build --target jkdesktop` → 0 errors.
Run: `powershell -File tools/probes/probe_agent_e2e.ps1` → **7/7 PASS** (프로브가 permissions.json에 close allow를 쓰므로 서버 관문도 열림 — M1 동작 불변 확인).

- [ ] **Step 5: Commit**

```bash
git add engine/src/server/JKWindowServer.cpp engine/include/server/JKWindowServer.h
git commit -m "feat(server): window-client event subscription + server-side close_window gate (M2a)"
```

---

### Task 2: JKClientSurface — 창 클라이언트 에이전트 채널

**Files:**
- Modify: `engine/include/JKEvent.h` (AgentReply 이벤트 타입)
- Modify: `engine/include/client/JKClientSurface.h`
- Modify: `engine/src/client/JKClientSurface.cpp` (ReadLoop ~298행 뒤 분기 + 전송 API)

**Interfaces:**
- Consumes: `ipc::WriteAgentJson`/`ReadAgentJson` (M1, AgentReply/AgentEvent 둘 다 파싱).
- Produces (Task 3/4/5가 사용):
  - `bool JKClientSurface::SendAgentQuery(uint32_t queryId, const std::string& json)`
  - `bool JKClientSurface::SendAgentEventSubscribe(bool subscribe)`
  - `bool JKClientSurface::PollAgentReply(AgentReply& out)` — `struct AgentReply { uint32_t queryId; bool ok; std::string json; }`
  - `size_t JKClientSurface::DrainAgentEvents(std::vector<std::string>& out)` — 이벤트 JSON 목록 drain

- [ ] **Step 1: JKEventType::AgentReply 추가**

`WindowListChanged` 뒤:
```cpp
// Agent channel (M2a): the server pushed an AgentReply or AgentEvent on the
// window connection. Pull with PollAgentReply / DrainAgentEvents.
AgentReply,
```

- [ ] **Step 2: 헤더 — 타입/멤버/API**

```cpp
#include <ipc/JKWireProtocol.h>
#include <deque>
#include <mutex>

// in class JKClientSurface (public section):
// Desktop Agent API over the window connection (M2a): the same pipe that
// carries CommitSurface/InputEvent also carries AgentQuery/Reply — the
// server's ProcessClientMessage answers any client.
struct AgentReply {
    uint32_t queryId = 0;
    bool ok = false;
    std::string json;
};
bool SendAgentQuery(uint32_t queryId, const std::string& json);
bool SendAgentEventSubscribe(bool subscribe);
bool PollAgentReply(AgentReply& out);
size_t DrainAgentEvents(std::vector<std::string>& out);

// private members:
std::deque<AgentReply> pendingAgentReplies_;
std::mutex agentReplyMutex_;
std::deque<std::string> pendingAgentEvents_;
std::mutex agentEventMutex_;
```

- [ ] **Step 3: 전송 API 구현**

```cpp
bool JKClientSurface::SendAgentQuery(uint32_t queryId, const std::string& json) {
    if (!IsConnected()) return false;
    return ipc::WriteAgentJson(*transport_, ipc::MsgType::AgentQuery,
                               queryId, 0, json);
}

bool JKClientSurface::SendAgentEventSubscribe(bool subscribe) {
    if (!IsConnected()) return false;
    ipc::AgentEventSubscribePayload payload{};
    payload.subscribe = subscribe ? 1 : 0;
    return ipc::WriteMessage(*transport_, ipc::MsgType::AgentEventSubscribe,
                             &payload, sizeof(payload));
}
```

- [ ] **Step 4: ReadLoop 분기 (ShellRegisterAck 분기 뒤)**

```cpp
} else if (msg.type == ipc::MsgType::AgentReply ||
           msg.type == ipc::MsgType::AgentEvent) {
    // Desktop Agent API (M2a): coalesce into per-kind queues and wake the
    // app; it polls PollAgentReply/DrainAgentEvents on the frame loop.
    uint32_t queryId = 0, ok = 0;
    std::string json;
    if (ipc::ReadAgentJson(msg, queryId, ok, json)) {
        JKEvent ev{};
        ev.type = JKEventType::AgentReply;
        if (msg.type == ipc::MsgType::AgentReply) {
            AgentReply reply{ queryId, ok != 0, std::move(json) };
            std::lock_guard<std::mutex> lock(agentReplyMutex_);
            if (pendingAgentReplies_.size() >= 64) pendingAgentReplies_.pop_front();
            pendingAgentReplies_.push_back(std::move(reply));
            ev.keyCode = queryId;   // reply wake carries its query id
        } else {
            std::lock_guard<std::mutex> lock(agentEventMutex_);
            if (pendingAgentEvents_.size() >= 256) pendingAgentEvents_.pop_front();
            pendingAgentEvents_.push_back(std::move(json));
        }
        QueueInputEvent(ev);
    }
}
```

- [ ] **Step 5: 폴 API 구현**

```cpp
bool JKClientSurface::PollAgentReply(AgentReply& out) {
    std::lock_guard<std::mutex> lock(agentReplyMutex_);
    if (pendingAgentReplies_.empty()) return false;
    out = std::move(pendingAgentReplies_.front());
    pendingAgentReplies_.pop_front();
    return true;
}

size_t JKClientSurface::DrainAgentEvents(std::vector<std::string>& out) {
    std::lock_guard<std::mutex> lock(agentEventMutex_);
    out.clear();
    while (!pendingAgentEvents_.empty()) {
        out.push_back(std::move(pendingAgentEvents_.front()));
        pendingAgentEvents_.pop_front();
    }
    return out.size();
}
```

- [ ] **Step 6: 빌드 + 회귀**

Run: `cmake --build build --target jkdesktop jkagentd` → 0 errors.
Run: `./build/jkdesktop.exe test` → 0 failures (단일 프로세스 경로 회귀).

- [ ] **Step 7: Commit**

```bash
git add engine/include/JKEvent.h engine/include/client/JKClientSurface.h engine/src/client/JKClientSurface.cpp
git commit -m "feat(client): agent query/event channel on JKClientSurface (M2a)"
```

---

### Task 3: 팔레트 앱 스켈레톤

**Files:**
- Create: `engine/include/apps/ClientPaletteApp.h`
- Create: `engine/src/apps/ClientPaletteApp.cpp`
- Create: `engine/src/apps/JKAppModule_palette.cpp`
- Modify: `engine/CMakeLists.txt` (taskmgr 블록 ~394행 뒤)

**Interfaces:**
- Consumes: Task 2의 JKClientSurface 채널, JKClientApplication(OnInit/PreProcessMessage/RenderOverlay 훅 — lesson 5: Timer에서 frameDirty_ 재무장).
- Produces: `ClientPaletteApp`(title "Command Palette" — Task 6이 타이틀로 매칭), `jkapp_palette.dll`, `--client palette` 스폰.

- [ ] **Step 1: CMake 타깃**

taskmgr 블록 뒤:
```cmake
add_library(jkapp_palette SHARED
    src/apps/JKAppModule_palette.cpp
    src/apps/ClientPaletteApp.cpp
)
target_compile_definitions(jkapp_palette PRIVATE JKAPP_MODULE_BUILD)
target_link_libraries(jkapp_palette PRIVATE jkcore imgui)
set_target_properties(jkapp_palette PROPERTIES PREFIX "")
```
(jkx 팩/런처 아이콘 없음 — 팔레트는 셸 부속 유틸리티로 taskbar처럼 `.jkx` 아님.)

- [ ] **Step 2: 모듈 엔트리** — taskmgr 복제, 메타만 교체:

```cpp
JKAPP_EXPORT const jk::JKAppMeta* jk_app_meta() {
    static const jk::JKAppMeta meta{ "palette", "Command Palette", 520, 360 };
    return &meta;
}
JKAPP_EXPORT int jk_app_run_client(const char* pipeName) {
    const jk::JKAppMeta* meta = jk_app_meta();
    jk::ClientPaletteApp app;
    if (!app.Init(meta->title, meta->width, meta->height, pipeName)) return 1;
    return app.Run();
}
```

- [ ] **Step 3: 앱 클래스 스켈레톤** — ClientPaletteApp.h/cpp. taskmgr 구조 그대로 (Timer 재무장/ImGui 초기화/OnClose 정리), UI는 입력 박스 + 로그:

`ClientPaletteApp.h`:
```cpp
#ifndef APPS_CLIENTPALETTEAPP_H
#define APPS_CLIENTPALETTEAPP_H

#include <client/JKClientApplication.h>
#include <client/JKClientSurface.h>
#include <chrono>
#include <string>
#include <vector>

namespace jk {

// Command palette (agent platform M2a, spec §6.2): an ImGui window client
// that speaks the Desktop Agent API over its own window connection. Slash
// commands are deterministic; natural-language delegation is a later stage.
// It doubles as the minimal notification center: AgentEventSubscribe feeds
// desktop events into the panel below the input box.
class ClientPaletteApp : public JKClientApplication {
public:
    ~ClientPaletteApp() override;

protected:
    void OnInit() override;
    void OnClose() override;
    bool PreProcessMessage(const JKEvent& ev) override;
    bool IsFrameDirty() const override { return frameDirty_; }
    void OnFrameCommitted() override;
    void RenderOverlay(SDL_Renderer* renderer, int w, int h) override;

private:
    void BuildUi(int w, int h);
    void AppendLog(const std::string& line);

    bool frameDirty_ = true;
    bool imguiReady_ = false;
    bool scrollDirty_ = false;
    bool focusInput_ = true;   // grab keyboard focus on open
    std::chrono::steady_clock::time_point lastFrame_;

    char input_[256] = {};
    std::vector<std::string> log_;
};

} // namespace jk
#endif // APPS_CLIENTPALETTEAPP_H
```

`ClientPaletteApp.cpp` — 스켈레톤 버전:
```cpp
// Command palette client (agent platform M2a, spec §6.2). See
// ClientPaletteApp.h. Structure mirrors ClientTaskmgrApp (docs/23 §11.2).
#include <apps/ClientPaletteApp.h>

#include <imgui_impl_jkwindow.h>
#include <JKWindow.h>
#include <SDL.h>
#include <cstdio>
#include <cctype>

namespace jk {
namespace {
class PaletteRoot : public JKWindow {
public:
    explicit PaletteRoot(const std::string& title) : JKWindow(title) {}
    void OnPaintClient(JKDC& dc) override {
        const JKRect client = GetClientRect();
        dc.SetColor(24, 24, 30, 255);
        dc.FillRect(client);
    }
};
std::string Trim(const std::string& s) {
    size_t b = 0, e = s.size();
    while (b < e && std::isspace(static_cast<unsigned char>(s[b]))) ++b;
    while (e > b && std::isspace(static_cast<unsigned char>(s[e - 1]))) --e;
    return s.substr(b, e - b);
}
} // namespace

ClientPaletteApp::~ClientPaletteApp() = default;

void ClientPaletteApp::OnInit() {
    auto main = std::make_unique<PaletteRoot>("Command Palette");
    main->SetWindowRect(JKRect{ 0, 0, 520, 360 });
    main->SetAttrFlags(WA_CHROMELESS);
    SetMainWindow(std::move(main));
    SetTimerInterval(16);
    ImGui::CreateContext();
    ImGui::GetIO().IniFilename = nullptr;
    lastFrame_ = std::chrono::steady_clock::now();
    AppendLog("command palette ready - type /help");
}

void ClientPaletteApp::OnClose() {
    if (imguiReady_) {
        ImGui_ImplJKWindow_Shutdown();
        ImGui::DestroyContext();
        imguiReady_ = false;
    }
}

bool ClientPaletteApp::PreProcessMessage(const JKEvent& ev) {
    ImGui_ImplJKWindow_ProcessJKEvent(ev);
    if (ev.type == JKEventType::Timer) frameDirty_ = true;
    return true;
}

void ClientPaletteApp::OnFrameCommitted() { frameDirty_ = false; }

void ClientPaletteApp::RenderOverlay(SDL_Renderer* renderer, int w, int h) {
    if (!imguiReady_) {
        if (!ImGui_ImplJKWindow_Init(renderer)) return;
        imguiReady_ = true;
    }
    const auto now = std::chrono::steady_clock::now();
    const float dt = std::chrono::duration<float>(now - lastFrame_).count();
    lastFrame_ = now;
    ImGui_ImplJKWindow_NewFrame(dt, w, h);
    ImGui::NewFrame();
    BuildUi(w, h);
    ImGui::Render();
    ImGui_ImplJKWindow_RenderDrawData(ImGui::GetDrawData(), renderer);
}

void ClientPaletteApp::BuildUi(int w, int h) {
    ImGui::SetNextWindowPos(ImVec2(0, 0));
    ImGui::SetNextWindowSize(ImVec2((float)w, (float)h));
    if (ImGui::Begin("palette", nullptr,
                     ImGuiWindowFlags_NoDecoration | ImGuiWindowFlags_NoMove)) {
        if (focusInput_) {
            ImGui::SetKeyboardFocusHere();
            focusInput_ = false;
        }
        if (ImGui::InputText("##cmd", input_, sizeof(input_),
                             ImGuiInputTextFlags_EnterReturnsTrue)) {
            const std::string text = Trim(input_);
            input_[0] = '\0';
            if (!text.empty()) {
                AppendLog("> " + text);
                if (text == "/help") {
                    AppendLog("  /list  /launch <app>  /close <id>");
                    AppendLog("  /save <name>  /restore <name>  /undo");
                } else {
                    AppendLog("  (agent queries arrive in the next task)");
                }
            }
            ImGui::SetKeyboardFocusHere();  // stay in the box
            focusInput_ = true;
        }
        ImGui::BeginChild("log", ImVec2(0, 0));
        for (const auto& line : log_) ImGui::TextWrapped("%s", line.c_str());
        if (scrollDirty_) {
            ImGui::SetScrollHereY(1.0f);
            scrollDirty_ = false;
        }
        ImGui::EndChild();
    }
    ImGui::End();
}

void ClientPaletteApp::AppendLog(const std::string& line) {
    log_.push_back(line);
    scrollDirty_ = true;
}

} // namespace jk
```
(UI 텍스트는 ASCII — ImGui 기본 폰트에 한글 글리프 없음, Global Constraints.)

- [ ] **Step 4: 빌드 + 스모크**

Run: `cmake --build build --target jkapp_palette jkdesktop`.
Run (engine/build): `./jkdesktop.exe --server &` (백그라운드) → sleep 4 → `./jkdesktop.exe --client palette &` → sleep 3 →
`./jkdesktop.exe agentctl '{"tool":"list_windows","args":{}}'` → **"Command Palette"가 목록에 보여야 함** → 서버 프로세스 종료.

- [ ] **Step 5: Commit**

```bash
git add engine/CMakeLists.txt engine/include/apps/ClientPaletteApp.h engine/src/apps/ClientPaletteApp.cpp engine/src/apps/JKAppModule_palette.cpp
git commit -m "feat(palette): ImGui command palette app skeleton (M2a)"
```

---

### Task 4: 슬래시 커맨드 + 에이전트 쿼리 + undo 칩

**Files:**
- Modify: `engine/include/apps/ClientPaletteApp.h`
- Modify: `engine/src/apps/ClientPaletteApp.cpp`

**Interfaces:**
- Consumes: Task 2 채널 (SendAgentQuery/PollAgentReply), Task 1의 서버 관문(permission_denied 응답).
- Produces: 결정적 커맨드 전체 — `/list /launch /close /save /restore /undo /help`; `pre_undo` 스냅샷 관례.

- [ ] **Step 1: 헤더 — 커맨드 상태 추가**

```cpp
// private:
void Submit(const std::string& text);
uint32_t SendTool(const std::string& tool, const std::string& argsJson); // 0 = not sent
void PumpReplies();
static std::string EscapeJson(const std::string& in);

uint32_t nextQueryId_ = 1;
uint32_t pendingQueryId_ = 0;
// Destructive commands (close/restore) save "pre_undo" first and run when
// the save reply lands (spec §9: undo = layout snapshot).
std::string afterUndoSave_;      // "tool\x1fargsJson" pending behind the save
uint32_t undoSaveQueryId_ = 0;
bool undoSaved_ = false;
```

- [ ] **Step 2: 구현** — 스켈레톤의 Enter 분기를 `Submit(text)`로 교체하고 아래 추가:

```cpp
std::string ClientPaletteApp::EscapeJson(const std::string& in) {
    std::string out;
    for (char c : in) {
        if (c == '"' || c == '\\') { out += '\\'; out += c; }
        else if (static_cast<unsigned char>(c) < 0x20) {
            char buf[8];
            std::snprintf(buf, sizeof(buf), "\\u%04x", c);
            out += buf;
        } else out += c;
    }
    return out;
}

uint32_t ClientPaletteApp::SendTool(const std::string& tool,
                                    const std::string& argsJson) {
    JKClientSurface* surface = Surface();
    if (!surface || !surface->IsConnected()) {
        AppendLog("! not connected to the desktop");
        return 0;
    }
    if (pendingQueryId_ != 0) {
        AppendLog("! busy (previous command still running)");
        return 0;
    }
    const std::string json =
        "{\"tool\":\"" + tool + "\",\"args\":" + argsJson + "}";
    const uint32_t id = nextQueryId_++;
    if (!surface->SendAgentQuery(id, json)) {
        AppendLog("! send failed");
        return 0;
    }
    pendingQueryId_ = id;
    return id;
}

void ClientPaletteApp::Submit(const std::string& text) {
    if (text.empty()) return;
    if (text[0] != '/') {
        AppendLog("  natural-language delegation arrives in a later stage");
        return;
    }
    const size_t sp = text.find(' ');
    const std::string cmd = text.substr(1, sp == std::string::npos
                                             ? std::string::npos : sp - 1);
    const std::string arg = (sp == std::string::npos)
                                ? std::string() : Trim(text.substr(sp + 1));
    if (cmd == "help") {
        AppendLog("  /list  /launch <app>  /close <id>");
        AppendLog("  /save <name>  /restore <name>  /undo");
    } else if (cmd == "list") {
        SendTool("list_windows", "{}");
    } else if (cmd == "launch") {
        if (arg.empty()) AppendLog("  usage: /launch <app>");
        else SendTool("launch_app", "{\"app\":\"" + EscapeJson(arg) + "\"}");
    } else if (cmd == "save") {
        if (arg.empty()) AppendLog("  usage: /save <name>");
        else SendTool("save_layout", "{\"name\":\"" + EscapeJson(arg) + "\"}");
    } else if (cmd == "restore") {
        if (arg.empty()) AppendLog("  usage: /restore <name>");
        else {
            const uint32_t id = SendTool(
                "save_layout", "{\"name\":\"pre_undo\"}");
            if (id) {
                undoSaveQueryId_ = id;
                afterUndoSave_ = "restore_layout\x1f" + arg;
            }
        }
    } else if (cmd == "close") {
        const int id = arg.empty() ? 0 : std::atoi(arg.c_str());
        if (id <= 0) {
            AppendLog("  usage: /close <id>  (see /list)");
        } else {
            const uint32_t save = SendTool(
                "save_layout", "{\"name\":\"pre_undo\"}");
            if (save) {
                undoSaveQueryId_ = save;
                afterUndoSave_ = "close_window\x1f" + arg;
            }
        }
    } else if (cmd == "undo") {
        if (!undoSaved_) AppendLog("  nothing to undo");
        else SendTool("restore_layout", "{\"name\":\"pre_undo\"}");
    } else {
        AppendLog("  unknown command - /help");
    }
}

void ClientPaletteApp::PumpReplies() {
    JKClientSurface* surface = Surface();
    JKClientSurface::AgentReply reply;
    while (surface && surface->PollAgentReply(reply)) {
        if (reply.queryId == pendingQueryId_) pendingQueryId_ = 0;
        if (reply.queryId == undoSaveQueryId_ && !afterUndoSave_.empty()) {
            // The pre_undo save landed — now run the destructive command
            // it was guarding (sequential because the palette is 1-deep).
            if (reply.ok) {
                undoSaved_ = true;
                const size_t sep = afterUndoSave_.find('\x1f');
                const std::string tool = afterUndoSave_.substr(0, sep);
                const std::string arg = afterUndoSave_.substr(sep + 1);
                if (tool == "close_window") {
                    SendTool(tool, "{\"id\":" + arg + "}");
                } else {
                    SendTool(tool, "{\"name\":\"" + EscapeJson(arg) + "\"}");
                }
            }
            afterUndoSave_.clear();
            undoSaveQueryId_ = 0;
            continue;
        }
        AppendLog("  " + reply.json);
    }
}
```
RenderOverlay에서 `ImGui::NewFrame()` 전에 `PumpReplies()` 호출 추가. close가 permission_denied면 스냅샷만 남고 안전 (다음 /undo가 직전 상태 복원).

- [ ] **Step 3: 빌드 + 수동 스모크 (사용자 확인 항목)**

Run: `cmake --build build --target jkapp_palette`; 서버+팔레트 실행 후 수동 확인:
`/list` → 창 목록 JSON 표시, `/launch minesweeper` → 창 생성, `/close <id>` → 닫힘(permissions.json 없으면 permission_denied 표시 — 서버 관문 동작), `/save work` → `/launch minesweeper` → `/restore work`, `/undo`.

- [ ] **Step 4: Commit**

```bash
git add engine/include/apps/ClientPaletteApp.h engine/src/apps/ClientPaletteApp.cpp
git commit -m "feat(palette): slash commands over the agent channel + undo chip (M2a)"
```

---

### Task 5: 이벤트 피드 (알림 센터 최소판)

**Files:**
- Modify: `engine/include/apps/ClientPaletteApp.h`
- Modify: `engine/src/apps/ClientPaletteApp.cpp`

**Interfaces:**
- Consumes: Task 1 완화된 이벤트 푸시 + Task 2의 DrainAgentEvents; `jk::agent::AgentJson`(jkcore, M1).
- Produces: 팔레트 하단 이벤트 패널 — window.created/destroyed/focused 실시간 표시 (스펙 §7 알림 센터의 최소판; 독립 앱은 트리거 스크립트와 함께 M2b 이후).

- [ ] **Step 1: 구독 + 피드 상태**

헤더 private에:
```cpp
void DrainEvents();
std::vector<std::string> feed_;   // latest desktop events (bounded)
```
OnInit 마지막에 (스켈레톤의 AppendLog 앞):
```cpp
if (JKClientSurface* surface = Surface()) {
    surface->SendAgentEventSubscribe(true);
}
```

- [ ] **Step 2: DrainEvents 구현 + UI**

```cpp
void ClientPaletteApp::DrainEvents() {
    JKClientSurface* surface = Surface();
    std::vector<std::string> events;
    if (!surface || surface->DrainAgentEvents(events) == 0) return;
    for (const std::string& e : events) {
        agent::AgentJson json(e);
        std::string topic, title;
        int id = 0;
        json.GetStr("topic", topic);
        json.GetStr("title", title);
        json.GetInt("id", id);
        if (topic.empty()) continue;
        feed_.push_back(topic + ": " +
                        (title.empty() ? "?" : title) +
                        " (#" + std::to_string(id) + ")");
    }
    while (feed_.size() > 12) feed_.erase(feed_.begin());
}
```
RenderOverlay에서 PumpReplies 옆에 `DrainEvents()` 호출. BuildUi의 log child 높이를 줄이고(예: `ImVec2(0, -GetFrameHeightWithSpacing()*7)`), 그 아래:
```cpp
ImGui::Separator();
ImGui::Text("events");
ImGui::BeginChild("feed", ImVec2(0, 0), ImGuiChildFlags_Borders);
for (const auto& line : feed_) ImGui::TextWrapped("%s", line.c_str());
ImGui::EndChild();
```

- [ ] **Step 3: 빌드 + 스모크**

Run: `cmake --build build --target jkapp_palette`. 서버+팔레트 실행 → `/launch minesweeper` → 이벤트 패널에 `window.created: Minesweeper (#N)` 표시 확인.

- [ ] **Step 4: Commit**

```bash
git add engine/include/apps/ClientPaletteApp.h engine/src/apps/ClientPaletteApp.cpp
git commit -m "feat(palette): live desktop event feed (notification center MVP, M2a)"
```

---

### Task 6: Alt+Space 서버 핫키 — 스폰 또는 재활성화

**Files:**
- Modify: `engine/src/server/JKWindowServer.cpp` (SDL_KEYDOWN 분기 ~977행 + TogglePalette 추가)
- Modify: `engine/include/server/JKWindowServer.h`

**Interfaces:**
- Consumes: Task 3의 팔레트 (title `"Command Palette"` 매칭), SpawnClient, FocusClient(호출자 clientsMutex_ 보유 계약).
- Produces: `void JKWindowServer::TogglePalette();` — Alt+Space 1회 = 팔레트 1개(500ms 스폰 스로틀이 중복 클릭 방지).

- [ ] **Step 1: 키 분기 인터셉트**

```cpp
} else if (ev.type == SDL_KEYDOWN || ev.type == SDL_KEYUP) {
    // Alt+Space opens (or refocuses) the command palette (M2a, spec §6.2).
    if (ev.type == SDL_KEYDOWN && !ev.key.repeat &&
        (ev.key.keysym.mod & KMOD_ALT) &&
        ev.key.keysym.sym == SDLK_SPACE) {
        TogglePalette();
        return;
    }
    ...existing code...
```

- [ ] **Step 2: TogglePalette 구현**

```cpp
// Alt+Space (M2a): bring the palette to front if it is already open,
// otherwise spawn it. Title-match — the palette is a regular client whose
// surface title is the stable key.
void JKWindowServer::TogglePalette() {
    bool found = false;
    {
        std::lock_guard<std::mutex> lock(clientsMutex_);
        for (auto& c : clients_) {
            if (!c || c->IsDisconnected() || c->IsControlOnly() || c->IsShell()) {
                continue;
            }
            if (c->Title() == "Command Palette") {
                if (compositor_) compositor_->SetLayerVisible(c->Id(), true);
                FocusClient(c->Id());  // caller-holds-clientsMutex_ contract
                found = true;
                break;
            }
        }
    }
    if (found) {
        PushWindowList();  // taskbar active highlight follows
    } else {
        SpawnClient("palette");
    }
}
```
헤더에 `void TogglePalette();` 선언.

- [ ] **Step 3: 빌드 + 스모크**

Run: `cmake --build build --target jkdesktop`. 서버 실행 → **Alt+Space** → 팔레트 등장, 다시 Alt+Space → 같은 창 재활성화(새 창 아님 — 서버 로그에 spawn 두 번째 없음 확인).

- [ ] **Step 4: Commit**

```bash
git add engine/src/server/JKWindowServer.cpp engine/include/server/JKWindowServer.h
git commit -m "feat(server): Alt+Space spawns or refocuses the command palette (M2a)"
```

---

### Task 7: 프로브 + 문서 + 상태 갱신

**Files:**
- Create: `engine/tools/probes/probe_agent_palette.ps1`
- Create: `docs/30_desktop_agent_m2a.md`
- Modify: `docs/superpowers/specs/2026-09-09-desktop-agent-platform-design.md` (헤더 상태줄)
- Modify: `docs/29_desktop_agent_m1.md` §7 (M2a 연결 한 줄)

**Interfaces:**
- Consumes: 전체. 팔레트의 stderr는 Start-Process로 캡처되지 않으므로 관측은 (a) 서버 로그(스폰/표면), (b) `agentctl list_windows`의 "Command Palette" 엔트리, (c) state 폴더의 layout 파일로 한다.

- [ ] **Step 1: probe_agent_palette.ps1**

```powershell
# M2a probe: the palette exists as a window client on the agent platform.
$ErrorActionPreference = "Continue"
$exe = "I:\progwork\JKENGINE\engine\build\jkdesktop.exe"
Get-Process jkdesktop -ErrorAction SilentlyContinue | Stop-Process -Force
Start-Sleep -Seconds 1

Start-Process -FilePath $exe -ArgumentList "--server" `
    -WorkingDirectory (Split-Path $exe) -WindowStyle Hidden
Start-Sleep -Seconds 4

# Spawn the palette directly (Alt+Space spawns the same way — Task 6).
Start-Process -FilePath $exe -ArgumentList "--client","palette" `
    -WorkingDirectory (Split-Path $exe) -WindowStyle Hidden
Start-Sleep -Seconds 3

# list_windows through the agent API — the palette is a regular window.
$list = '{"tool":"list_windows","args":{}}' | & $exe agentctl
$list = ($list -join "`n")

# And the palette can talk: its ready line should appear in the palette's
# own trace when it processes the window list. Server log is the authority.
$log = Get-Content (Join-Path (Split-Path $exe) "jkdesktop_run.log") -Raw

Get-Process jkdesktop -ErrorAction SilentlyContinue | Stop-Process -Force

$ok = $true
if ($list -match 'Command Palette') { Write-Host "palette-in-list: PASS" }
else { $ok = $false; Write-Host "palette-in-list: FAIL $list" }
if ($log -match 'client surface \d+ created') { Write-Host "surface: PASS" }
else { $ok = $false; Write-Host "surface: FAIL" }

if ($ok) { Write-Host "PASS: agent palette"; exit 0 }
else { Write-Host "FAIL: agent palette"; exit 1 }
```
(서버 로그 파일명은 실제 실행 시 확인 후 수정 — `jkdesktop_run.log`는 M1의 read_log 기본값.)

- [ ] **Step 2: 전체 회귀**

Run: `./build/jkdesktop.exe test` → 0 failures; `./build/jkagentd.exe --selftest` → 0 failures; `probe_agent_e2e.ps1` → 7/7; `probe_agent_palette.ps1` → PASS.

- [ ] **Step 3: docs/30 + 상태 갱신 + Commit**

`docs/30_desktop_agent_m2a.md`: 아키텍처(팔레트 = 일반 창 클라 + 같은 연결 위 AgentQuery), Alt+Space 사용법, 커맨드 표(/list /launch /close /save /restore /undo), pre_undo 규칙, permissions.json = 두 얼굴(jkagentd + 팔레트)의 공통 관문, 제한(ImGui ASCII 폰트, NL 위임 미구현, 이벤트 종류 3개), M2b(트리거 스크립트) 연결.
스펙 헤더: "2단계 커맨드 바 MVP(M2a) 구현 완료 (2026-09-10); 트리거 스크립트(M2b)·채팅창 미착수"로 갱신.
docs/29 §7에 M2a 링크 추가.

```bash
git add engine/tools/probes/probe_agent_palette.ps1 docs/30_desktop_agent_m2a.md docs/29_desktop_agent_m1.md docs/superpowers/specs/2026-09-09-desktop-agent-platform-design.md
git commit -m "docs(agent): M2a command bar MVP — docs/30, probe, spec status"
git push
```

## 완료 조건 (사용자 확정 스코프)

1. Alt+Space → 팔레트 등장, 재입력 → 재활성화.
2. 팔레트에서 `/launch /list /close /save /restore /undo` 동작 — jkagentd(MCP)와 **같은 도구 집합**.
3. close는 permissions.json 관문(서버 측) 통과 — 기본 deny, 파일 편집이 승인.
4. 팔레트 이벤트 패널에 window.created/destroyed/focused 실시간 표시.
5. 파괴적 커맨드 전 자동 pre_undo 스냅샷 + `/undo` 복원.
6. 기존 회귀 전부 통과 (test / --selftest / e2e probe).