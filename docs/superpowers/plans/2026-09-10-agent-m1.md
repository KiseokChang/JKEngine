# jkdesktop Desktop Agent M1 구현 계획 (claude-wrapper)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Claude Code가 MCP stdio로 접속해 jkdesktop의 창을 조회·실행·포커스·종료하고 레이아웃을 저장/복원하며 데스크탑 이벤트를 읽을 수 있게 한다 (스펙 §6.1, M1).

**Architecture:** 윈도우 서버에 에이전트용 프로토콜 커맨드 4종(AgentQuery/AgentReply/AgentEventSubscribe/AgentEvent)을 추가한다. 페이로드는 "고정 헤더 + 가변 길이 UTF-8 JSON" — 기존 프로토콜의 POD 관행 위에 스펙 G7(JSON) 원칙을 접목. 제어 전용 연결(control-only client)은 화면 표면 없이 파이프로만 통신한다. jkagentd(사이드카)는 stdio MCP JSON-RPC를 서버 쿼리로 번역하고 receipt를 기록한다. `agentctl` CLI 모드는 사람/probe용 단발 쿼리 도구이자 서버 측 검증 하네스다.

**Tech Stack:** C++ (MinGW ucrt64/Ninja), 기존 `jkcore` 정적 라이브러리(파이프 전송·QuickJS JSON 파싱·ConPTY 포함), 신규 외부 의존성 없음. MCP 클라이언트는 Claude Code.

**Spec:** `docs/superpowers/specs/2026-09-09-desktop-agent-platform-design.md` (§3 API 표, §4 이벤트 버스, §5 권한, §6.1, Part 9-G7)

## Global Constraints

- 파이프 이름 상수: `"\\\\.\\pipe\\JKWindowServerPipe"` (`engine/src/main.cpp:2183` 기존 `kPipe` 재사용).
- JSON 파싱은 QuickJS `JS_ParseJSON` 패턴만 사용 (`engine/src/apps/JKTerminalConfig.cpp:83-93` 참조) — 새 서드파티 JSON 라이브러리 금지.
- 와이어 페이로드는 `#pragma pack(push,1)` POD + 가변 JSON 접미 (기존 `CommitSurfaceHeader`의 "Followed by N entries" 관행, `JKWireProtocol.h:81-85`).
- 빌드: `cd engine && export PATH="/c/msys64/ucrt64/bin:$PATH" && cmake --build build --target <target>`. I: 드라이브 MinGW 문제가 나오면 `engine/build_sdl2_jkwindow.bat` 경유.
- ConPTY 기반 코드는 Windows 전용 — POSIX 빌드는 스텁으로 컴파일 유지 (`JKConPtyBridge.cpp:225-237` 선례).
- 서버와 클라이언트는 모두 하나의 `jkdesktop.exe`다 (`--server` / `--client <name>`).
- 커밋은 태스크 단위. 커밋 메시지에 `Co-Authored-By: Claude Code <noreply@anthropic.com>` 포함.
- M1 비목표 (스펙 §11): 승인 프롬프트 UI 없음(`permissions.json` 파일로만 통제), 이벤트 rate limit 없음, 스폰된 앱의 크래시 감지 없음, 이벤트 버스 rate limit/dead-letter 없음(4단계 과제).

---

### Task 1: 와이어 프로토콜 — 에이전트 메시지 4종 추가

**Files:**
- Modify: `engine/include/ipc/JKWireProtocol.h`
- Modify: `engine/src/ipc/JKWireProtocol.cpp`

**Interfaces:**
- Produces: `ipc::MsgType::AgentQuery(17)/AgentReply(18)/AgentEventSubscribe(19)/AgentEvent(20)`, `ipc::WriteAgentJson(IWireTransport&, MsgType, uint32_t queryId, uint32_t ok, const std::string& json)`, `ipc::ReadAgentJson(const Message&, uint32_t& queryId, uint32_t& ok, std::string& json)`. 이후 모든 태스크가 이 두 함수를 쓴다.

- [ ] **Step 1: 메시지 타입 열거 추가**

`engine/include/ipc/JKWireProtocol.h:40`의 `WindowListSubscribe = 16` 뒤에 추가:

```cpp
    // Desktop Agent API (docs/superpowers/specs/2026-09-09-desktop-agent-platform-design.md §3).
    // 제어 전용 연결(표면/공유메모리 없음)이 파이프로만 통신한다.
    AgentQuery          = 17, // C -> S: 쿼리 (queryId + JSON)
    AgentReply          = 18, // S -> C: 쿼리 결과 (queryId + ok + JSON)
    AgentEventSubscribe = 19, // C -> S: AgentEvent 푸시 구독 {subscribe}
    AgentEvent          = 20  // S -> C: 데스크탑 이벤트 푸시 (JSON)
```

- [ ] **Step 2: 페이로드 구조체 + 직렬화 헬퍼 선언**

같은 헤더의 `WindowListSubscribePayload`(152행) 뒤에 추가:

```cpp
// AgentQuery/AgentReply/AgentEvent의 페이로드는 "고정 헤더 + jsonLen 바이트의
// UTF-8 JSON" — CommitSurfaceHeader의 "Followed by N entries" 관행과 동일.
#pragma pack(push, 1)
struct AgentQueryHeader {
    uint32_t queryId = 0;   // 요청-응답 상관 id
    uint32_t jsonLen  = 0;
};
struct AgentReplyHeader {
    uint32_t queryId = 0;
    uint32_t ok      = 0;   // 1 = 성공, 0 = 도구 수준 실패 (JSON에 error 포함)
    uint32_t jsonLen = 0;
};
struct AgentEventHeader {
    uint32_t jsonLen = 0;
};
#pragma pack(pop)
struct AgentEventSubscribePayload {
    uint32_t subscribe = 0;
};

// AgentQuery/AgentReply/AgentEvent 공용 작성기. type이 셋 중 하나가 아니면
// false. json이 비어도 헤더만 실어 보낸다.
bool WriteAgentJson(IWireTransport& transport, MsgType type,
                    uint32_t queryId, uint32_t ok, const std::string& json);

// 위 세 타입의 Message에서 헤더+JSON을 꺼낸다. 타입이 아니거나 잘리면 false.
bool ReadAgentJson(const Message& msg, uint32_t& queryId, uint32_t& ok,
                   std::string& json);
```

- [ ] **Step 3: 구현**

`engine/src/ipc/JKWireProtocol.cpp` 끝에 추가 (상단에 `#include <cstring>` 필요):

```cpp
bool WriteAgentJson(IWireTransport& transport, MsgType type,
                    uint32_t queryId, uint32_t ok, const std::string& json) {
    if (json.size() > 0x7FFFFFFFull) return false;
    const uint32_t jsonLen = static_cast<uint32_t>(json.size());
    std::vector<uint8_t> payload;
    if (type == MsgType::AgentQuery) {
        AgentQueryHeader h{queryId, jsonLen};
        payload.resize(sizeof(h) + jsonLen);
        std::memcpy(payload.data(), &h, sizeof(h));
    } else if (type == MsgType::AgentReply) {
        AgentReplyHeader h{queryId, ok, jsonLen};
        payload.resize(sizeof(h) + jsonLen);
        std::memcpy(payload.data(), &h, sizeof(h));
    } else if (type == MsgType::AgentEvent) {
        AgentEventHeader h{jsonLen};
        payload.resize(sizeof(h) + jsonLen);
        std::memcpy(payload.data(), &h, sizeof(h));
    } else {
        return false;
    }
    if (jsonLen)
        std::memcpy(payload.data() + payload.size() - jsonLen,
                    json.data(), jsonLen);
    return WriteMessage(transport, type, payload);
}

bool ReadAgentJson(const Message& msg, uint32_t& queryId, uint32_t& ok,
                   std::string& json) {
    queryId = 0; ok = 0; json.clear();
    size_t headerLen = 0;
    if (msg.type == MsgType::AgentQuery)       headerLen = sizeof(AgentQueryHeader);
    else if (msg.type == MsgType::AgentReply)  headerLen = sizeof(AgentReplyHeader);
    else if (msg.type == MsgType::AgentEvent)  headerLen = sizeof(AgentEventHeader);
    else return false;
    if (msg.payload.size() < headerLen) return false;
    uint32_t jsonLen = 0;
    if (msg.type == MsgType::AgentQuery) {
        AgentQueryHeader h; std::memcpy(&h, msg.payload.data(), sizeof(h));
        queryId = h.queryId; jsonLen = h.jsonLen;
    } else if (msg.type == MsgType::AgentReply) {
        AgentReplyHeader h; std::memcpy(&h, msg.payload.data(), sizeof(h));
        queryId = h.queryId; ok = h.ok; jsonLen = h.jsonLen;
    } else {
        AgentEventHeader h; std::memcpy(&h, msg.payload.data(), sizeof(h));
        jsonLen = h.jsonLen;
    }
    if (msg.payload.size() != headerLen + jsonLen) return false;
    json.assign(reinterpret_cast<const char*>(msg.payload.data()) + headerLen, jsonLen);
    return true;
}
```

- [ ] **Step 4: 빌드 + 회귀 확인**

Run: `cd engine && export PATH="/c/msys64/ucrt64/bin:$PATH" && cmake --build build --target jkdesktop && ./build/jkdesktop.exe test`
Expected: 빌드 성공, 기존 셀프테스트 통과(실패 0).

- [ ] **Step 5: Commit**

```bash
git add engine/include/ipc/JKWireProtocol.h engine/src/ipc/JKWireProtocol.cpp
git commit -m "feat(ipc): agent control-plane message types (AgentQuery/Reply/EventSubscribe/Event)"
```

---

### Task 2: 서버 제어 연결 + 쿼리 스켈린톤, JKAgentClient, agentctl

**Files:**
- Modify: `engine/include/server/JKClientConnection.h` (controlOnly 플래그)
- Modify: `engine/src/server/JKWindowServer.cpp` + `engine/include/server/JKWindowServer.h` (핸드셰이크 분기 + AgentQuery 디스패치)
- Create: `engine/include/agent/JKAgentClient.h`, `engine/src/agent/JKAgentClient.cpp`
- Modify: `engine/CMakeLists.txt` (jkcore 소스에 JKAgentClient.cpp 추가), `engine/src/main.cpp` (agentctl 모드)

**Interfaces:**
- Consumes: Task 1의 `WriteAgentJson`/`ReadAgentJson`.
- Produces: `jk::agent::JKAgentClient` — `bool Connect(const std::string& pipeName = "\\\\.\\pipe\\JKWindowServerPipe")`, `bool Query(const std::string& tool, const std::string& argsJson, std::string& replyJsonOut)`, `bool SubscribeEvents(bool subscribe)`, `size_t PollEvents(std::vector<AgentEvent>& out)`. CLI: `jkdesktop agentctl '<json>'`, `jkdesktop agent-events <초>`. 서버는 최소 도구 `ping`을 답한다.

- [ ] **Step 1: 서버가 제어 전용 연결을 받아들이게 한다**

먼저 읽을 코드: `JKWindowServer::AcceptorLoop` (`engine/src/server/JKWindowServer.cpp:211-276`) — accept 직후 Hello를 읽고 `SetPid`한 뒤 `CreateSurface`를 기다린다. `JKClientConnection` (`engine/include/server/JKClientConnection.h:97-122`) — 표면/공유메모리 필드.

1. `JKClientConnection.h`에 필드/접근자 추가:
```cpp
    // 제어 전용 연결: 표면·공유메모리·컴포지터 레이어가 없다 (에이전트 브로커).
    bool IsControlOnly() const { return controlOnly_; }
    void SetControlOnly(bool c) { controlOnly_ = c; }
private:
    bool controlOnly_ = false;
```
2. `AcceptorLoop`에서 Hello 처리·`SetPid` 직후(약 `:250`)에 두 번째 메시지를 읽어 분기:
```cpp
    ipc::Message second;
    if (!ipc::ReadMessage(*transport, second)) { /* 기존 실패 처리와 동일 */ }
    if (second.type == ipc::MsgType::AgentEventSubscribe) {
        // 제어 전용 연결: 표면 생성을 건너뛰고 클라이언트 목록에만 등록.
        client->SetControlOnly(true);
        ipc::AgentEventSubscribePayload sub;
        std::memcpy(&sub, second.payload.data(),
                    std::min(second.payload.size(), sizeof(sub)));
        client->SetAgentEventSubscriber(sub.subscribe != 0);  // 아래 3 참고
        // 컨테이너 등록·로그는 기존 CreateSurface 경로의 등록 코드를 그대로 재사용
        // (구현자는 이 지점의 실제 등록 코드를 먼저 읽고 동일하게 연결한다).
        continue;  // 다음 accept로
    }
    // second가 CreateSurface면 기존 경로 그대로 (second를 기존 코드에 전달).
```
3. `JKClientConnection.h`에 `bool agentEventSubscriber_ = false;` + 접근자 `AgentEventSubscriber()/SetAgentEventSubscriber(bool)` 추가. 컨트롤 클라이언트가 레이어를 만들지 않으므로 `CleanupDisconnectedClients`와 `PushWindowListUnsafe`(`JKWindowServer.cpp:1118-1152`, 이미 `shell_` 제외 패턴 있음)의 순회에서 `IsControlOnly()`를 스킵해야 한다 — 두 함수와, 클라이언트 메시지를 큐에 넣는 읽기 경로(구현자가 `ProcessPendingMessages` `:999-1009`로 역추적)에서 제외/포함 여부를 확인해 `controlOnly`를 올바르게 분기한다.
4. `ProcessClientMessage`(`JKWindowServer.cpp:1011`) 체인에 추가:
```cpp
    } else if (msg.type == ipc::MsgType::AgentEventSubscribe) {
        ipc::AgentEventSubscribePayload p;
        std::memcpy(&p, msg.payload.data(), std::min(msg.payload.size(), sizeof(p)));
        client.SetAgentEventSubscriber(p.subscribe != 0);
    } else if (msg.type == ipc::MsgType::AgentQuery) {
        uint32_t qid = 0, okDummy = 0; std::string json;
        if (ipc::ReadAgentJson(msg, qid, okDummy, json))
            HandleAgentQuery(client, qid, json);
    }
```
5. `JKWindowServer.h`에 선언하고 `.cpp`에 구현 (응답은 반드시 항상 보낸다 — 클라이언트가 블로킹 Read이므로):
```cpp
void JKWindowServer::HandleAgentQuery(JKClientConnection& client,
                                      uint32_t queryId, const std::string& json) {
    // M1 스켈린톤: ping만, 나머지는 Task 3/4에서 채운다.
    std::string reply;
    if (json.find("\"ping\"") != std::string::npos) {
        reply = "{\"ok\":true,\"pong\":true}";
    } else {
        reply = "{\"ok\":false,\"error\":\"not_implemented\"}";
    }
    ipc::WriteAgentJson(*client.Transport(), ipc::MsgType::AgentReply,
                        queryId, 1, reply);
}
```
(`Transport()` 접근자가 없으면 `JKClientConnection`에 `ipc::IWireTransport& Transport()` 추가 — `transport_` 유니크포인터 반환.)

- [ ] **Step 2: JKAgentClient 라이브러리**

`engine/include/agent/JKAgentClient.h`:

```cpp
#ifndef JKAGENTCLIENT_H
#define JKAGENTCLIENT_H
#include <ipc/JKWireProtocol.h>
#include <ipc/JKPipeTransport.h>
#include <deque>
#include <memory>
#include <string>
#include <vector>

namespace jk {
namespace agent {

struct AgentEvent {
    std::string topic;  // "window.created" 등 — JSON의 "topic" 필드
    std::string json;   // 원본 이벤트 JSON
};

// 윈도우 서버의 제어 전용 클라이언트. agentctl과 jkagentd가 공유한다.
class JKAgentClient {
public:
    bool Connect(const std::string& pipeName = "\\\\.\\pipe\\JKWindowServerPipe");
    bool IsConnected() const;
    // 요청 {"tool":"<tool>","args":<argsJson>} → 응답 JSON(그대로 반환).
    // 서버는 항상 응답하므로 블로킹 대기가 안전하다 (M1, 타임아웃 없음).
    bool Query(const std::string& tool, const std::string& argsJson,
               std::string& replyJsonOut);
    bool SubscribeEvents(bool subscribe);
    size_t PollEvents(std::vector<AgentEvent>& out);  // 큐 비우기
private:
    bool PumpMessages();   // 도착한 메시지 읽어 응답/이벤트로 분류 (논블로킹 아님 — ReadMessage 1회)
    std::unique_ptr<ipc::JKPipeTransport> transport_;
    uint32_t nextQueryId_ = 1;
    std::deque<std::string> pendingReplies_;
    std::deque<AgentEvent> eventQueue_;
};
} // namespace agent
} // namespace jk
#endif
```

`engine/src/agent/JKAgentClient.cpp` — 구현 요령: `Query`는 요청 JSON을 만들어(`{"tool":"` + tool + `","args":` + argsJson + `}`) `WriteAgentJson(..., AgentQuery, queryId, 0, req)`로 보내고, `AgentReply`가 올 때까지 `ReadMessage`를 돌며 `queryId` 일치 대기 — 도착한 `AgentEvent`는 `eventQueue_`에 적립. `SubscribeEvents`는 `AgentEventSubscribePayload{subscribe}`를 전송. `PollEvents`는 큐를 out으로 옮긴다. 파이프가 닫히면 `ReadMessage`가 false를 반환하므로 그 즉시 false.

- [ ] **Step 3: agentctl / agent-events CLI 모드**

`engine/src/main.cpp` — headless 툴 패턴(`jkx-list`, `:2044`)을 따라 `RunJkxExtract` 근처에 추가:

```cpp
// agentctl: 서버에 에이전트 쿼리 1건을 보내고 응답 JSON을 stdout에 출력.
// 사용: jkdesktop agentctl "{\"tool\":\"ping\",\"args\":{}}"
static int RunAgentCtl(const char* requestJson) {
    jk::agent::JKAgentClient agent;
    if (!agent.Connect()) { std::fprintf(stderr, "agentctl: connect failed\n"); return 2; }
    std::string reply;
    if (!agent.QueryRaw(requestJson, reply)) {
        std::fprintf(stderr, "agentctl: query failed\n"); return 3;
    }
    std::fputs(reply.c_str(), stdout);
    std::fputs("\n", stdout);
    return 0;
}

// agent-events: N초간 이벤트를 구독해 줄 단위 JSON으로 출력 (probe용).
static int RunAgentEvents(int seconds);
```
`JKAgentClient`에 `QueryRaw(전체 요청 JSON)` 변형을 추가한다 (`Query`는 `QueryRaw`를 감싼 편의 함수). `main()`의 `test-script` 분기(`:2021`) 근처에 배치:
```cpp
    if (argc > 1 && std::strcmp(argv[1], "agentctl") == 0) {
        if (argc < 3) { std::fprintf(stderr, "Usage: jkdesktop agentctl '<json>'\n"); return 1; }
        return RunAgentCtl(argv[2]);
    }
    if (argc > 1 && std::strcmp(argv[1], "agent-events") == 0) {
        if (argc < 3) { std::fprintf(stderr, "Usage: jkdesktop agent-events <seconds>\n"); return 1; }
        return RunAgentEvents(std::atoi(argv[2]));
    }
```
`RunAgentEvents`: Connect → SubscribeEvents(true) → `SDL_GetTicks` 아닌 `std::chrono::steady_clock`으로 N초 동안 폴링 루프(50ms sleep + `PollEvents`에서 이벤트를 한 줄씩 stdout) → 0 반환. `--help` 사용법 목록(`:1986-2013`)에 두 줄 추가.

- [ ] **Step 4: CMake**

`engine/CMakeLists.txt` jkcore 소스 목록(`:153-177` 근처, ipc 파일들 뒤)에:
```cmake
    src/agent/JKAgentClient.cpp
```

- [ ] **Step 5: 빌드 + 실서버 검증**

Run:
```bash
cd engine && export PATH="/c/msys64/ucrt64/bin:$PATH" && cmake --build build --target jkdesktop
cd build && ./jkdesktop.exe --server > srv.log 2>&1 &
sleep 3
./jkdesktop.exe agentctl '{"tool":"ping","args":{}}'
```
Expected: `{"ok":true,"pong":true}` 출력. 이어서:
```bash
./jkdesktop.exe agentctl '{"tool":"list_windows","args":{}}'
```
Expected: `{"ok":false,"error":"not_implemented"}` (Task 3 전까지 정상). 검증 후 서버 종료 (`kill %1`).

- [ ] **Step 6: Commit**

```bash
git add engine/include/agent engine/src/agent engine/include/server engine/src/server engine/src/main.cpp engine/CMakeLists.txt
git commit -m "feat(agent): server control-plane skeleton + JKAgentClient + agentctl CLI"
```

---

### Task 3: 서버 도구 — list_windows / focus_window / close_window + 이벤트 푸시

**Files:**
- Modify: `engine/src/server/JKWindowServer.cpp`, `engine/include/server/JKWindowServer.h`
- Modify: `engine/src/main.cpp` (agentctl은 그대로 — 검증만)

**Interfaces:**
- Consumes: Task 2의 `HandleAgentQuery` 골격, `JKClientConnection`의 `Id()/Title()/Pid()/x_/y_/width_/height_/IsControlOnly()/AgentEventSubscriber()`.
- Produces: 도구 응답 규격 — `list_windows` → `{"ok":true,"windows":[{"id":1,"title":"...","pid":123,"x":100,"y":50,"w":800,"h":600,"focused":true,"minimized":false}]}`; `focus_window`/`close_window` (args `{"id":N}`) → `{"ok":true}` 또는 `{"ok":false,"error":"window_not_found"}`. 이벤트 푸시: `{"topic":"window.created|window.destroyed|window.focused","id":N,"title":"...","pid":N,"ts":<epoch ms>}`.

- [ ] **Step 1: list_windows 구현**

`HandleAgentQuery`에 도구 분기 추가. 창 열거는 `PushWindowListUnsafe`(`JKWindowServer.cpp:1118-1152`)의 필드 매핑을 재사용하되 JSON으로 — 제어 클라이언트(`IsControlOnly()`)와 셸(`shell_`)은 제외:
```cpp
    if (tool == "list_windows") {
        std::string out = "{\"ok\":true,\"windows\":[";
        bool first = true;
        for (auto& c : clients_) {              // 실제 컨테이너 이름은 구현자가 PushWindowListUnsafe에서 확인
            if (c->IsControlOnly() || c->IsShell()) continue;
            char item[512];
            std::snprintf(item, sizeof(item),
                "%s{\"id\":%u,\"title\":\"%s\",\"pid\":%u,\"x\":%d,\"y\":%d,"
                "\"w\":%d,\"h\":%d,\"focused\":%s,\"minimized\":%s}",
                first ? "" : ",", c->Id(), JsonEsc(c->Title()).c_str(), c->Pid(),
                c->X(), c->Y(), c->Width(), c->Height(),
                (focusedClientId_ == c->Id()) ? "true" : "false",
                compositor_->IsLayerVisible(c->Id()) ? "false" : "true");
            out += item; first = false;
        }
        reply = out + "]}";
    }
```
제목에 `"`/`\` 대응용 짧은 이스케이프 헬퍼 `JsonEsc(const std::string&)`를 `JKWindowServer.cpp` 파일 스코프에 추가 (`"` → `\"`, `\` → `\\`, 제어문자 → `\uXXXX` 최소한). 멤버 접근자(x_/y_/width_/height_)가 없으면 `JKClientConnection`에 const 접근자 추가.

- [ ] **Step 2: focus_window — 기존 WindowActivate 분기 재사용**

`ProcessClientMessage`의 WindowActivate 처리(`:1068-1080`)를 `FocusAndRestore(uint32_t id)` 헬퍼로 추출하고, `HandleAgentQuery`의 `focus_window`에서 표면 존재 확인 후 호출. 응답은 `{"ok":true}` / `{"ok":false,"error":"window_not_found"}`.

- [ ] **Step 3: close_window — 서버 주도 종료**

클라이언트가 자기 스스로 보내는 `Close`(`:1037`)와 달리 서버가 해당 클라이언트의 transport로 `MsgType::Close`를 전송한다. 클라이언트 읽기 루프(`JKClientSurface.cpp:199`)가 Close를 받으면 종료하므로, 이후 일반 disconnect 정리 경로가 뒤를 처리한다:
```cpp
    if (tool == "close_window") {
        // args에서 id를 파싱(Step 4 헬퍼 이전이므로 우선 문자열 검색은 금지 —
        // 아래 Step 4의 AgentJsonArgs를 여기서 함께 도입한다)
        ...
        ipc::WriteMessage(*target->Transport(), ipc::MsgType::Close, std::vector<uint8_t>{});
        PushAgentEvent("window.destroyed", target->Id(), target->Title(), target->Pid());
    }
```

- [ ] **Step 4: args JSON 파싱 헬퍼 + PushAgentEvent**

`engine/include/agent/JKAgentJson.h` (헤더 온리, 서버·jkagentd 공용) — QuickJS 폐기 런타임 패턴(`JKTerminalConfig.cpp:83-93`):
```cpp
// throwaway QuickJS runtime으로 JSON 객체 필드를 읽는 최소 헬퍼.
// 사용 후 파서가 스코프를 벗어나면 모든 JSValue가 해제된다.
namespace jk { namespace agent {
class AgentJson {
public:
    explicit AgentJson(const std::string& text);   // ok() == 파싱 성공
    ~AgentJson();
    bool ok() const { return ok_; }
    bool GetInt(const char* key, int& out) const;
    bool GetStr(const char* key, std::string& out) const;
private:
    JSRuntime* rt_ = nullptr; JSContext* ctx_ = nullptr; JSValue root_;
    bool ok_ = false;
};
}}
```
구현은 `JS_NewRuntime`/`JS_NewContext` → NUL 종료 버퍼 복사 → `JS_ParseJSON(ctx, buf, len-1, "args")` → `JS_GetPropertyStr`로 `int32`/`string` 추출. `HandleAgentQuery`가 먼저 `AgentJson(json)`으로 `tool`을 읽도록 골격을 갱신한다(문자열 검색 제거).

`JKWindowServer`에 이벤트 푸시 추가:
```cpp
void JKWindowServer::PushAgentEvent(const char* topic, uint32_t id,
                                    const std::string& title, uint32_t pid) {
    long long ts = (long long)(std::time(nullptr)) * 1000;  // <ctime>
    char buf[512];
    std::snprintf(buf, sizeof(buf), "{\"topic\":\"%s\",\"id\":%u,\"title\":\"%s\",\"pid\":%u,\"ts\":%lld}",
                  topic, id, JsonEsc(title).c_str(), pid, ts);
    for (auto& c : clients_)
        if (c->IsControlOnly() && c->AgentEventSubscriber())
            ipc::WriteAgentJson(*c->Transport(), ipc::MsgType::AgentEvent, 0, 1, buf);
}
```
호출 지점: (a) 표면 생성 완료(AcceptorLoop의 SurfaceCreated 회신 직후, `window.created`), (b) `CleanupDisconnectedClients`(`:1250` 근처, `window.destroyed`), (c) `FocusAndRestore`(`window.focused`).

- [ ] **Step 5: 빌드 + 실서버 검증**

Run:
```bash
cmake --build build --target jkdesktop
cd build && ./jkdesktop.exe --server > srv.log 2>&1 &
sleep 3
./jkdesktop.exe --client minesweeper > /dev/null 2>&1 &
sleep 2
./jkdesktop.exe agentctl '{"tool":"list_windows","args":{}}'
```
Expected: `"title":"Minesweeper"`(실제 타이틀 문자열로 확인) 포함 JSON. 이어서:
```bash
ID=$(./jkdesktop.exe agentctl '{"tool":"list_windows","args":{}}' | grep -o '"id":[0-9]*' | head -1 | cut -d: -f2)
./jkdesktop.exe agentctl "{\"tool\":\"focus_window\",\"args\":{\"id\":$ID}}"
./jkdesktop.exe agentctl "{\"tool\":\"close_window\",\"args\":{\"id\":$ID}}"
sleep 2
./jkdesktop.exe agentctl '{"tool":"list_windows","args":{}}'
```
Expected: focus/close 응답 `"ok":true`, 마지막 목록에서 해당 id 소멸. (grep/cut이 어려우면 타이틀로 직접 조회해도 된다.) 검증 후 서버 종료.

- [ ] **Step 6: Commit**

```bash
git add engine/include/agent engine/src/server engine/include/server engine/src/main.cpp
git commit -m "feat(agent): list/focus/close tools + desktop event pushes"
```

---

### Task 4: 서버 도구 — launch_app / save_layout / restore_layout

**Files:**
- Modify: `engine/src/server/JKWindowServer.cpp` (HandleAgentQuery 분기 3개 + 레이아웃 파일 I/O), `engine/include/server/JKWindowServer.h`

**Interfaces:**
- Consumes: Task 2의 SpawnClient 경로(`JKWindowServer.cpp:1515-1594`), Task 3의 `AgentJson`/`JsonEsc`.
- Produces: `launch_app` (args `{"app":"minesweeper"}` 또는 `{"jkx":"경로"}`) → `{"ok":true}`; `save_layout` (args `{"name":"work"}`) → `{"ok":true,"count":N}`; `restore_layout` (args `{"name":"work"}`) → `{"ok":true,"restored":N,"unmatched":["제목",...]}`. 레이아웃 파일: `<exeDir>/state/layout_<name>.json`.

- [ ] **Step 1: launch_app**

`SpawnClient(appName, fromJkx)` 호출로 끝낸다 (500ms 스폰 스로틀은 그대로 둔다 — 응답은 즉시):
```cpp
    if (tool == "launch_app") {
        std::string app, jkx;
        args.GetStr("app", app); args.GetStr("jkx", jkx);
        if (!app.empty())      SpawnClient(app.c_str(), false);
        else if (!jkx.empty()) SpawnClient(jkx.c_str(), true);
        else { reply = "{\"ok\":false,\"error\":\"missing_app\"}"; }
    }
```

- [ ] **Step 2: save_layout / restore_layout — 레이아웃 스냅샷**

서버에 레이아웃 저장 코드가 없음이 확인됐다(탐색 결과 3번 — 신규 코드). exe 디렉터리 계산은 `SpawnClient`(`:1559`)의 `GetModuleFileNameA` 패턴 재사용:

```cpp
std::string JKWindowServer::StateDir() const {   // <exeDir>/state, 없으면 생성
    char exePath[MAX_PATH] = {}; GetModuleFileNameA(nullptr, exePath, MAX_PATH);
    std::string dir = exePath; size_t slash = dir.find_last_of("\\/");
    dir = (slash == std::string::npos) ? std::string(".") : dir.substr(0, slash);
    dir += "\\state";
    CreateDirectoryA(dir.c_str(), nullptr);
    return dir;
}
```
save — 제어 클라이언트·셸 제외하고 `x_,y_,width_,height_,title_`를 기록:
```cpp
    if (tool == "save_layout") {
        std::string name; args.GetStr("name", name);
        std::string json = "{\"name\":\"" + JsonEsc(name) + "\",\"windows\":[";
        bool first = true;
        for (auto& c : clients_) {
            if (c->IsControlOnly() || c->IsShell()) continue;
            char item[512];
            std::snprintf(item, sizeof(item),
                "%s{\"title\":\"%s\",\"x\":%d,\"y\":%d,\"w\":%d,\"h\":%d}",
                first ? "" : ",", JsonEsc(c->Title()).c_str(),
                c->X(), c->Y(), c->Width(), c->Height());
            json += item; first = false;
        }
        json += "]}";
        std::FILE* f = std::fopen((StateDir() + "\\layout_" + name + ".json").c_str(), "wb");
        if (!f) { reply = "{\"ok\":false,\"error\":\"write_failed\"}"; }
        else { std::fwrite(json.data(), 1, json.size(), f); std::fclose(f);
               reply = "{\"ok\":true,\"count\":" + std::to_string(count) + "}"; }
    }
```
restore — 파일을 읽어 `windows` 배열을 순회, 타이틀로 클라이언트를 찾아 `compositor_->SetLayerPosition(id, x, y)` + `client->SetPosition(x, y)`(`JKClientConnection.h:54`). 매칭 실패 항목은 `unmatched` 배열에 타이틀 수집. 배열 순회는 `AgentJson`에 `GetArraySize(const char* key, size_t&)`/`GetIntAt(const char* key, size_t idx, const char* field, int&)`/`GetStrAt(...)` 3개 메서드를 추가해 처리.

- [ ] **Step 3: 빌드 + 실서버 검증 (정복 사다리 첫 사이클)**

Run:
```bash
cmake --build build --target jkdesktop
cd build && ./jkdesktop.exe --server > srv.log 2>&1 &
sleep 3
./jkdesktop.exe agentctl '{"tool":"launch_app","args":{"app":"minesweeper"}}'
sleep 2
./jkdesktop.exe agentctl '{"tool":"save_layout","args":{"name":"probe"}}'
./jkdesktop.exe agentctl '{"tool":"restore_layout","args":{"name":"probe"}}'
cat state/layout_probe.json
```
Expected: launch/save/restore 모두 `"ok":true`, `layout_probe.json`에 minesweeper 항목 존재. 검증 후 서버 종료.

- [ ] **Step 4: Commit**

```bash
git add engine/src/server engine/include/server engine/include/agent
git commit -m "feat(agent): launch_app + layout save/restore (state/*.json)"
```

---

### Task 5: jkagentd — MCP stdio 코어 (서버 독립, 셀프테스트)

**Files:**
- Create: `engine/tools/jkagentd/main.cpp`
- Modify: `engine/CMakeLists.txt` (신규 실행파일 타깃)

**Interfaces:**
- Produces: `jkagentd.exe` — stdin/stdout으로 newline-delimited JSON-RPC 2.0(MCP stdio 전송 규격). 메서드: `initialize`, `notifications/initialized`(무시), `tools/list`, `tools/call`. `--selftest` 모드는 스크립트된 입력으로 자체 검증하고 실패 수를 exit code로 반환.

- [ ] **Step 1: 실패하는 셀프테스트 먼저**

`engine/tools/jkagentd/main.cpp`에 프레임과 함께:
```cpp
// jkagentd: Desktop Agent MCP 브로커 (스펙 §2 — 사이드카).
// stdin: MCP JSON-RPC 줄 단위 → 서버 파이프 쿼리 → stdout: JSON-RPC 응답.
#include <agent/JKAgentClient.h>
#include <cstdio>
#include <string>
#include <vector>

namespace {

std::string JsonEsc(const std::string& s);   // Task 3과 동일 규칙 — 이 파일에 로컬 복제

// JSON-RPC 한 줄을 처리해 응답 줄(또는 "" for notification)을 만든다.
// 테스트 가능성이 핵심 — 실제 stdin 루프와 --selftest가 같은 함수를 쓴다.
std::string HandleLine(const std::string& line, bool& isResponse);

int RunSelfTest() {
    int failures = 0;
    bool isResp = false;
    std::string r = HandleLine(
        "{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"initialize\",\"params\":{}}", isResp);
    if (!isResp || r.find("\"protocolVersion\"") == std::string::npos) ++failures;
    r = HandleLine("{\"jsonrpc\":\"2.0\",\"method\":\"notifications/initialized\"}", isResp);
    if (isResp || !r.empty()) ++failures;
    r = HandleLine("{\"jsonrpc\":\"2.0\",\"id\":2,\"method\":\"tools/list\"}", isResp);
    if (!isResp || r.find("list_windows") == std::string::npos
                || r.find("terminal_exec") == std::string::npos) ++failures;
    r = HandleLine("{\"jsonrpc\":\"2.0\",\"id\":3,\"method\":\"tools/call\","
                   "\"params\":{\"name\":\"nope\",\"arguments\":{}}}", isResp);
    if (!isResp || r.find("unknown_tool") == std::string::npos) ++failures;
    r = HandleLine("not json", isResp);
    if (!isResp || r.find("-32700") == std::string::npos) ++failures;
    std::fprintf(stderr, "selftest: %d failures\n", failures);
    return failures;
}

} // namespace

int main(int argc, char** argv) {
    if (argc > 1 && std::string(argv[1]) == "--selftest") return RunSelfTest();
    std::string line;
    while (std::getline(std::cin, line)) {
        if (line.empty()) continue;
        bool isResp = false;
        std::string out = HandleLine(line, isResp);
        if (!out.empty()) { std::fputs(out.c_str(), stdout); std::fputc('\n', stdout); std::fflush(stdout); }
    }
    return 0;
}
```
이 시점의 `HandleLine`은 항상 파싱 실패(-32700)만 반환하는 스텁 — 셀프테스트가 실패하는 것을 먼저 확인한다.

Run: `cmake --build build --target jkagentd && ./build/jkagentd.exe --selftest`
Expected: `selftest: 4 failures` — 스텁이 항상 -32700을 반환하므로 "not json" 케이스만 즉시 통과한다.

- [ ] **Step 2: HandleLine 구현**

JSON 파싱은 `jk::agent::AgentJson`(Task 4) 재사용, 응답 조립은 수기 직렬화:
```cpp
std::string HandleLine(const std::string& line, bool& isResponse) {
    isResponse = true;
    jk::agent::AgentJson req(line);
    if (!req.ok())
        return "{\"jsonrpc\":\"2.0\",\"id\":null,\"error\":{\"code\":-32700,\"message\":\"parse error\"}}";
    std::string method; req.GetStr("method", method);
    int id = 0; bool hasId = req.GetInt("id", id);
    auto result = [&](const std::string& r) {
        return "{\"jsonrpc\":\"2.0\",\"id\":" + std::to_string(id) + ",\"result\":" + r + "}";
    };
    if (method == "initialize")
        return result("{\"protocolVersion\":\"2024-11-05\",\"capabilities\":{\"tools\":{}},"
                      "\"serverInfo\":{\"name\":\"jkagentd\",\"version\":\"0.1.0\"}}");
    if (method.rfind("notifications/", 0) == 0) { isResponse = false; return ""; }
    if (method == "tools/list") {
        // 9개 도구 — name/description/inputSchema를 여기에 정적 테이블로 기술.
        // (list_windows, launch_app, close_window, focus_window, save_layout,
        //  restore_layout, read_log, read_events, terminal_exec)
        return result(kToolsListJson);   // const char* 상수 — 파일 상단에 전체 JSON 기입
    }
    if (method == "tools/call") {
        std::string tool; req.GetStr("name", tool);   // params 내부 접근은
        // AgentJson에 GetObject("params")/그 객체에서 GetStr("name") 2단 접근을 추가해 사용.
        if (g_toolImpl.find(tool) == g_toolImpl.end())
            return result("{\"ok\":false,\"error\":\"unknown_tool\"}");
        return result(g_toolImpl[tool](argsJson));    // Task 6에서 실제 구현으로 교체
    }
    return "{\"jsonrpc\":\"2.0\",\"id\":" + std::to_string(id) +
           ",\"error\":{\"code\":-32601,\"message\":\"method not found\"}}";
}
```
`kToolsListJson`에는 9개 도구의 `{"name":...,"description":...,"inputSchema":{"type":"object","properties":...}}`를 완전하게 기입한다(각 도구의 인자는 Task 2~4·6·7의 args 규격과 동일). `tools/call`의 서버 호출부는 Task 6까지 `{"ok":false,"error":"not_connected"}` 스텁.

- [ ] **Step 3: CMake 타깃**

`engine/CMakeLists.txt`의 `cefosr` 선언(`:417-421`) 근처에:
```cmake
# ---------------------------------------------------------------------------
# jkagentd: Desktop Agent MCP 브로커 (스펙 §2 사이드카).
# ---------------------------------------------------------------------------
add_executable(jkagentd tools/jkagentd/main.cpp)
target_link_libraries(jkagentd PRIVATE jkcore)
if(WIN32)
    target_link_options(jkagentd PRIVATE -static-libstdc++ -static-libgcc)
endif()
```

- [ ] **Step 4: 셀프테스트 통과**

Run: `cmake --build build --target jkagentd && ./build/jkagentd.exe --selftest`
Expected: `selftest: 0 failures`, exit code 0.

- [ ] **Step 5: Commit**

```bash
git add engine/tools/jkagentd engine/CMakeLists.txt
git commit -m "feat(jkagentd): MCP stdio core — initialize/tools-list/call routing with selftest"
```

---

### Task 6: jkagentd 서버 연결 — 7 도구 + receipt + 권한 + read_log/read_events

**Files:**
- Modify: `engine/tools/jkagentd/main.cpp`

**Interfaces:**
- Consumes: Task 2 `JKAgentClient`, Task 3/4의 도구 규격, Task 5의 `g_toolImpl` 라우팅.
- Produces: 모든 도구가 실동작. receipt는 `<exeDir>/state/receipts.jsonl` append-only. 권한은 exe 옆 `permissions.json` — `{"<tool>":"allow"|"deny"}`, 기본 allow, `close_window` 기본 deny (M1 헤드리스라 승인 프롬프트 불가 — 파일 수정으로 허용).

- [ ] **Step 1: 연결 + 도구 구현**

첫 `tools/call`에서 `JKAgentClient::Connect`를 지연 수행(lazy singleton)하고, 각 도구는 `Query(tool, argsJson)` 위임. `read_events`는 연결 직후 `SubscribeEvents(true)` 1회, 호출마다 `PollEvents`로 큐를 비워 `{"ok":true,"events":[...]}` 반환. 나머지 5개(list/launch/close/focus/save/restore)는 응답 JSON을 그대로 `content`로 감싼다:
```cpp
    // tools/call 성공 응답 포장 — MCP result.content[0].text에 도구 결과 JSON.
    return result("{\"content\":[{\"type\":\"text\",\"text\":\"" + JsonEsc(toolReply) + "\"}]}");
```

- [ ] **Step 2: read_log**

파일 경로: 환경변수 `JKDESKTOP_LOG`, 없으면 cwd의 `jkdesktop_run.log` (관행: `engine/run_test.sh:3`의 리다이렉션 대상). args `{"lines":N=50,"match":"정규식(선택)"}` — 파일을 끝에서 앞으로 줄 단위로 읽어 최대 N개, `std::regex` 필터. 파일이 없으면 `{"ok":false,"error":"log_file_missing"}`.

- [ ] **Step 3: receipt + permissions 게이트**

```cpp
// 모든 tools/call에 대해: (1) permissions.json 게이트 → deny면
// {"ok":false,"error":"permission_denied"} 결과로 종료.
// (2) 도구 실행 전 아니고 '기록이 목적'이므로 실행 후 결과와 함께 append:
//   {"ts":<epoch ms>,"tool":"close_window","args":{...},"ok":true}
// receipts.jsonl은 append("ab") — append-only 스펙(§5) 준수.
// permissions.json이 없으면 기본값(전부 allow, close_window만 deny) 사용.
```
`permissions.json` 파싱도 `AgentJson`. 게이트 결과는 도구 응답에 그대로 드러난다.

- [ ] **Step 4: E2E probe**

`engine/tools/probes/probe_agent_mcp.ps1` 생성 — probes 관행(`test_focus_spawn.ps1`의 시작-대기-판정 구조)을 따르되 픽셀 대신 JSON 판정:
```powershell
# PASS/FAIL 판정: jkagentd stdio 세션으로 launch→list→save→close→restore 전 경로
$exe  = "I:\progwork\JKENGINE\engine\build\jkdesktop.exe"
$agnt = "I:\progwork\JKENGINE\engine\build\jkagentd.exe"
Start-Process -FilePath $exe -ArgumentList "--server" -WindowStyle Hidden
Start-Sleep -Seconds 3
$session = '...
{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}
{"jsonrpc":"2.0","method":"notifications/initialized"}
{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"launch_app","arguments":{"app":"minesweeper"}}}
{"jsonrpc":"2.0","id":3,"method":"tools/call","params":{"name":"list_windows","arguments":{}}}
{"jsonrpc":"2.0","id":4,"method":"tools/call","params":{"name":"save_layout","arguments":{"name":"probe_e2e"}}}
'
$out = $session | & $agnt
if (($out -join "`n") -match '"ok":true' -and ($out -join "`n") -match 'Minesweeper') {
    Write-Host "PASS: agent mcp e2e"; exit 0
} else { Write-Host "FAIL: agent mcp e2e"; exit 1 }
```
(실제 구현 시 여러 줄 파이프 입력은 PowerShell 배열로 전달 — `$lines | & $agnt` 형태로 정리.)

Run: `powershell -ExecutionPolicy Bypass -File engine/tools/probes/probe_agent_mcp.ps1`
Expected: `PASS: agent mcp e2e`, 그리고 `engine/build/state/receipts.jsonl`에 레코드 4건.

- [ ] **Step 5: Commit**

```bash
git add engine/tools/jkagentd engine/tools/probes/probe_agent_mcp.ps1
git commit -m "feat(jkagentd): wire 7 tools to server, receipts, permissions gate, read_log/events + MCP e2e probe"
```

---

### Task 7: terminal_exec — ConPTY 스트리밍 도구

**Files:**
- Modify: `engine/tools/jkagentd/main.cpp`

**Interfaces:**
- Consumes: `jk::JKConPtyBridge` (`engine/include/terminal/JKConPtyBridge.h:23-63` — `Start(cmdline, cols, rows)`, `DrainOutput(out)`, `ShellExited()`, `Stop()`).
- Produces: `terminal_exec` args `{"command":"...","timeoutSec":30}` → `{"ok":true,"output":"...","ended":"exited|timeout"}`. POSIX 빌드는 `{"ok":false,"error":"unsupported_platform"}`.

- [ ] **Step 1: 구현**

```cpp
    if (tool == "terminal_exec") {
#if defined(_WIN32)
        std::string cmd; int timeoutSec = 30;
        params.GetStr("command", cmd); params.GetInt("timeoutSec", timeoutSec);
        jk::JKConPtyBridge pty;
        if (!pty.Start(cmd, 80, 25))
            return result("{\"ok\":false,\"error\":\"pty_start_failed\"}");
        std::string output; auto deadline = std::chrono::steady_clock::now()
                                       + std::chrono::seconds(timeoutSec);
        bool exited = false;
        while (std::chrono::steady_clock::now() < deadline) {
            std::string chunk; pty.DrainOutput(chunk); output += chunk;
            if (pty.ShellExited()) { exited = true; break; }
            Sleep(30);   // TerminalApp의 30ms 펌프 주기(TerminalApp.cpp:56)와 동일
        }
        pty.Stop();
        // 출력은 JSON 문자열로 이스케이프해 반환 (JsonEsc 재사용)
        reply = "{\"ok\":true,\"ended\":\"" + std::string(exited ? "exited" : "timeout")
              + "\",\"output\":\"" + JsonEsc(output) + "\"}";
#else
        reply = "{\"ok\":false,\"error\":\"unsupported_platform\"}";
#endif
    }
```
주의: `output`에 NUL이 오면 잘린다 — 필요시 치환. `JsonEsc`는 이스케이프에 제어문자 `\u000X` 처리를 포함해야 한다(Task 3의 최소 규격에 명시).

- [ ] **Step 2: 검증 (probe 세션에 한 도구 추가)**

Run:
```bash
printf '%s\n' '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}' \
  '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"terminal_exec","arguments":{"command":"cmd /c echo agent_pty_ok"}}}' \
  | ./build/jkagentd.exe
```
Expected: id 2의 응답 text에 `agent_pty_ok` 포함, `"ended":"exited"`.

- [ ] **Step 3: Commit**

```bash
git add engine/tools/jkagentd
git commit -m "feat(jkagentd): terminal_exec via ConPTY streaming"
```

---

### Task 8: claude-wrapper — .mcp.json + 사용 문서 + 최종 E2E

**Files:**
- Create: `.mcp.json` (저장소 루트)
- Create: `docs/29_desktop_agent_m1.md`
- Create: `engine/tools/probes/probe_agent_e2e.ps1`
- Modify: `docs/superpowers/specs/2026-09-09-desktop-agent-platform-design.md` (§6.1 완료 조건 체크 — 마지막에)

**Interfaces:**
- Consumes: Task 5-7의 완성된 jkagentd.
- Produces: Claude Code에서 `"mcp__jkdesktop__list_windows"` 등 9개 도구 사용 가능.

- [ ] **Step 1: .mcp.json**

저장소 루트에 (Claude Code가 자동 인식하는 프로젝트 스코프 MCP 설정):
```json
{
  "mcpServers": {
    "jkdesktop": {
      "command": "I:\\progwork\\JKENGINE\\engine\\build\\jkagentd.exe",
      "args": []
    }
  }
}
```

- [ ] **Step 2: 최종 E2E probe**

`engine/tools/probes/probe_agent_e2e.ps1` — Task 6의 probe에 `restore_layout`과 receipt 검증을 추가한 전체 시나리오(스펙 §6.1 완료 조건): 서버 기동 → launch_app(minesweeper) → list_windows → save_layout → close_window → list에서 소멸 확인 → restore_layout → `state/receipts.jsonl`에 레코드 ≥5건 확인. PASS/FAIL을 exit code로. (jkagentd 세션 한 번으로 연속 호출 — 연결이 서버보다 오래 살아있어야 하므로 세션 유지 방식은 Task 6의 probe와 동일.)

- [ ] **Step 3: 사용 문서**

`docs/29_desktop_agent_m1.md` — 번호 충돌 없음 확인(docs/의 마지막 일련번호는 27, engine/docs/28은 셸 프로토콜 문서). 내용: 아키텍처 한 다이어그램(스펙 §2 재사용), 서버 기동법, `agentctl` 사용례 5개, Claude Code 연결법(.mcp.json + 도구 목록), `permissions.json`/`receipts.jsonl` 위치와 기본값, 제한(승인 프롬프트 없음 — close_window 기본 deny, 서버 죽으면 쿼리 블로킹, POSIX에서 terminal_exec 미지원), 2단계(커맨드 바)로의 연결 한 문단.

- [ ] **Step 4: 스펙 상태 갱신**

`2026-09-09-desktop-agent-platform-design.md` 헤더의 상태 줄에 M1 구현 완료일과 커밋 범위를 한 줄 추가.

- [ ] **Step 5: 전체 회귀 + Commit**

Run: `./build/jkdesktop.exe test && ./build/jkagentd.exe --selftest && powershell -ExecutionPolicy Bypass -File engine/tools/probes/probe_agent_e2e.ps1`
Expected: self-test 0 실패, selftest 0 failures, `PASS`.

```bash
git add .mcp.json docs/29_desktop_agent_m1.md engine/tools/probes/probe_agent_e2e.ps1 docs/superpowers/specs/2026-09-09-desktop-agent-platform-design.md
git commit -m "feat(agent): claude-wrapper wiring (.mcp.json) + usage doc + final E2E probe — M1 complete"
```

---

## M2 설계 메모 (이 계획 범위 밖 — 사용자 지시 기록)

내부 에이전트 채팅창(2단계 커맨드 바/에이전트 창)은 **SDL 창 밖 — 적어도 JKWindow
체계 밖**에 있어야 한다. JKWindow 표면으로 만들지 않는다(채팅창이 데스크탑 컴포지터의
레이어가 되지 않음). 나중에 STT(음성 입력)로 확장될 수 있음을 감안한 제약 — 네이티브
OS 창/오디오 스택에 직접 붙는 별도 컴포넌트 방향. 2단계 설계 시 이 제약을 상위
요구사항으로 반영할 것.